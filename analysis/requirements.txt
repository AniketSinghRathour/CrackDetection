# NOTE: torch and torchvision are installed separately in the Dockerfile
# as CPU-only versions (~200MB instead of ~2GB GPU version).
# If running locally, install them manually:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# ── All versions pinned for numpy 1.x compatibility (required by torch 2.2.2) ──
fastapi
uvicorn
opencv-python-headless>=4.8,<4.10
numpy>=1.24,<2.0
python-multipart
scikit-image>=0.21,<0.23
scipy>=1.11,<1.14
requests
langchain-core
langchain-google-genai
langgraph
python-dotenv